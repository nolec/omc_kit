---
skill_name: omc-plan
description: "구현 전 계획·설계·TDD 태스크 분해. 트리거: 계획해줘, 설계해줘, 분해해줘, 어떻게 구현할지, 태스크 나눠줘. RED→GREEN→VERIFY 단계로 분해. omc-task 실행 전 반드시 사용."
---

# OMC 설계·계획

> **이 스킬을 쓰면 안 되는 상황**:
> - 요구사항이 아직 모호 → `$omc-brainstorm` 먼저
> - 방향 검증이 필요 → `$omc-office-hours` 먼저
> - 범위 재검토 필요 → `$omc-ceo-review` 먼저

---

## Phase 0: 현재 상태 확인

```bash
python3 scripts/omc.py state status --target .
```

---

## Phase 1: 요구사항 인터뷰 (답을 모두 채울 것)

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PLAN — 요구사항
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
목표: 이 작업으로 무엇을 달성하는가?
  → _______________

범위 (포함):
  → _______________

범위 (제외 — 명시적으로):
  → _______________

DoD (완료 기준): 언제 "완료"로 볼 수 있는가?
  → _______________

제약: 건드리면 안 되는 것, 지켜야 할 패턴
  → _______________

☐ 사용자 컨펌 완료
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## Phase 2: 최소 설계

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PLAN — 설계
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
입력: (파라미터, 타입, 예시값)
  → _______________

출력: (반환값, 타입, 예시값)
  → _______________

실패·에러 정책:
  - 입력이 잘못됐을 때: _______________
  - 외부 의존성 실패 시: _______________

영향받는 기존 파일:
  → _______________
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## Phase 3: TDD 태스크 분해 (MANDATORY)

각 태스크마다 RED→GREEN→VERIFY 순서로 작성한다.

```
태스크 1: [기능 설명]
  RED    : [작성할 실패 테스트 — 파일명 + 케이스 설명]
  GREEN  : [테스트를 통과하는 최소 구현 — 파일명]
  VERIFY : [확인 커맨드]

태스크 2: ...
```

---

## Phase 4: OMC 세션 기록

```bash
python3 scripts/omc.py state confirm --target .
```

☐ confirm 완료 → `$omc-task` 진입 가능

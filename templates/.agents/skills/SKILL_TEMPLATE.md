---
skill_name: omc-[NAME]
description: "[한 줄 설명]. 트리거: [트리거 키워드1], [트리거 키워드2]. [핵심 제약 1줄]."
---

# [스킬 제목]

> **이 스킬을 쓰면 안 되는 상황**:
> - [상황 1] → [대체 스킬]
> - [상황 2] → [대체 스킬]

---

## Step 0: 컨텍스트 수집

> **AI는 아래 커맨드를 직접 실행하고 결과를 확인한 후 다음 단계로 진입한다. 건너뛰지 않는다.**

```bash
# [컨텍스트 설명]
python3 scripts/omc.py state status --target . 2>/dev/null
git log --oneline -5 2>/dev/null
git branch --show-current 2>/dev/null
# 필요에 따라 추가 커맨드
```

수집 결과 연결:
- `omc.py state status` → **항목 1** ([해당 출력 항목명]) 에 반영
- `git log` → **항목 N** ([해당 출력 항목명]) 에 반영

데이터가 없거나 커맨드 결과가 비어있으면 해당 항목에 "없음"으로 명시한다. 빈칸으로 두지 않는다.

---

## [실행 섹션 제목]

> (이 섹션에도 커맨드가 있으면 아래 지시문을 추가하고, 없으면 섹션 전체를 삭제)
> **AI는 아래 커맨드를 직접 실행하고 결과를 확인한다. 건너뛰지 않는다.**

```bash
# 예시 커맨드 2>/dev/null
```

### 출력 포맷

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[스킬명] RESULT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. [항목 제목]
   ([설명])
   → _______________

2. [항목 제목]
   ([설명])
   없으면 "없음" 으로 명시
   → _______________

3. 다음 액션 제안
   판단 기준:
   - [조건 A] → [제안 A]
   - [조건 B] → [제안 B]
   → _______________
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

☐ [완료 체크박스 — 코드블록 밖에 위치]

---

## 이후 액션

| 결과 | 다음 스킬 |
|---|---|
| [결과 A] | `$omc-[다음 스킬]` |
| [결과 B] | `$omc-[다른 스킬]` |

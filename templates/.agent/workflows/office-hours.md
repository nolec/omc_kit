# /office-hours — 제품 사고 먼저 (코딩 전)

기능 구현 요청이 들어왔을 때 **코드를 먼저 짜지 않습니다.**
아래 6개 강제 질문에 답하지 않으면 구현 단계로 넘어갈 수 없습니다.

---

## 전제 조건

```bash
python3 scripts/omc_guard.py require --for "office-hours" --target .
```

---

## 6개 강제 질문 (빈칸을 채우세요)

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
OFFICE HOURS — 제품 사고 먼저
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Q1. 이 기능을 쓸 사람이 정확히 누구인가?
    ("모든 사용자"는 답이 아닙니다 — 구체적인 한 명을 묘사하세요)
    → _______________________________________________

Q2. 지금 그 사람이 겪는 실제 고통은 무엇인가?
    → _______________________________________________

Q3. 이 기능이 완성됐을 때 성공 기준은 무엇인가?
    (측정 가능한 지표로)
    → _______________________________________________

Q4. 가장 단순한 구현으로 이 고통을 해결할 수 있는가?
    (MVP를 한 문장으로)
    → _______________________________________________

Q5. 이 기능에서 명시적으로 제외하는 것은 무엇인가?
    → _______________________________________________

Q6. 10점짜리 버전은 어떤 모습인가?
    → _______________________________________________

☐ 6개 질문 답변 완료
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## 이 커맨드 후 → `/plan`으로 이어가기

Office Hours 없이 `/plan`이나 `/task`로 바로 가는 것은 권장하지 않습니다.

---

요청 내용: $ARGUMENTS

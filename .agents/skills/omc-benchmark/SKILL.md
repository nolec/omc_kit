---
skill_name: omc-benchmark
description: "대상 기능을 세계 1등 제품과 비교해 갭 분석 + 차별화 포인트 도출. 트리거: 세계 1등이랑 비교해줘, 벤치마크, 경쟁사 분석, 우리가 더 잘할 수 있는 게 뭐야. 비교 대상은 사용자가 직접 지정한다."
---

# OMC Benchmark — 세계 1등과 비교 분석

> **이 스킬을 쓰면 안 되는 상황**:
> - 아직 기능 방향이 안 잡혔을 때 → `$omc-brainstorm` 또는 `$omc-office-hours` 먼저
> - 경쟁사 분석이 목적이 아닌 구현 계획 → `$omc-plan` 사용

> ⚠️ **AI 정보 신뢰도 경고 (필독)**
> 이 스킬의 벤치마크 결과는 **AI 학습 데이터 기준**입니다.
> 최신 기능, 최근 업데이트, 실제 UX 세부사항은 다를 수 있습니다.
> **중요한 결정 전 공식 문서·실제 제품을 직접 확인하세요.**

---

## Step 0: 컨텍스트 수집

> **AI는 아래 커맨드를 직접 실행하고 결과를 확인한 후 Step 1로 진입한다. 건너뛰지 않는다.**

```bash
python3 scripts/omc.py state status --target . 2>/dev/null
git branch --show-current 2>/dev/null
```

수집 결과 연결:
- `omc.py state status` → **Step 1** (분석 대상 기능 컨텍스트 파악) 에 반영
- `git branch` → **Step 1** (현재 브랜치명으로 작업 중인 기능 범위 추정) 에 반영

데이터가 없거나 커맨드 결과가 비어있으면 해당 항목에 "없음"으로 명시한다. 빈칸으로 두지 않는다.

---

## Step 1: 분석 대상 설정 (사용자 직접 입력 필수)

> **AI가 비교 대상을 임의로 결정하지 않는다. 아래 슬롯을 사용자가 채운 후에만 Step 2로 진입한다.**

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
BENCHMARK 설정
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
분석할 기능:
  → _______________
  (예: "상품 상세 페이지", "주문 관리 화면", "블록 에디터")

비교 기준 제품 (1등이라고 생각하는 제품, 직접 지정):
  → _______________
  (예: "Shopify", "Notion", "Linear", "Figma" 등 — 여러 개 가능)

비교 관점 (선택, 비워두면 AI가 전반적으로 분석):
  → _______________
  (예: "UX 흐름", "기능 완성도", "성능", "모바일 대응")
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

☐ Step 1 사용자 입력 완료

---

## Step 2: 갭 분석

> 각 항목에 답하기 어려우면 `N/A — [이유]` 형식으로 기재한다. 빈칸으로 두지 않는다.

> **AI 확신도 기준**:
> - 높음: AI 학습 데이터에서 명확히 확인된 기능 (공식 문서·영상 기반)
> - 중간: 여러 소스에서 언급됐으나 버전/변경 가능성 있음
> - 낮음: AI 추정 수준 — 반드시 직접 검증 후 판단

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
BENCHMARK RESULT
분석 기능  : _______________
비교 대상  : _______________
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[갭 분석 — 최소 3개 항목]

항목 1: [기능/UX 요소 이름]
  1등 수준   : _______________
  우리 수준  : _______________
  갭 판정   : 동등 / 열위 / 우위
  갭 크기   : 작음 / 중간 / 큼
  AI 확신도  : 높음 / 중간 / 낮음

항목 2: [기능/UX 요소 이름]
  1등 수준   : _______________
  우리 수준  : _______________
  갭 판정   : 동등 / 열위 / 우위
  갭 크기   : 작음 / 중간 / 큼
  AI 확신도  : 높음 / 중간 / 낮음

항목 3: [기능/UX 요소 이름]
  1등 수준   : _______________
  우리 수준  : _______________
  갭 판정   : 동등 / 열위 / 우위
  갭 크기   : 작음 / 중간 / 큼
  AI 확신도  : 높음 / 중간 / 낮음

(항목이 더 있으면 동일 형식으로 추가)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[차별화 포인트 — 1등보다 나아질 수 있는 것]

1등이 못 하는 것 또는 약한 것:
  → _______________

우리가 유리한 맥락 (도메인, 사용자군, 기술 스택):
  → _______________

차별화 제안 (구체적으로 1개):
  → _______________

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[우선순위 판단]

가장 먼저 메워야 할 갭 TOP 1:
  항목명 : _______________
  이유   : _______________
  (갭 크기가 크고 AI 확신도가 높은 항목을 우선 — 확신도 낮음이면 직접 검증 선행)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
⚠️ 위 분석은 AI 학습 데이터 기준입니다. AI 확신도 "낮음" 항목은 반드시 직접 검증 후 활용하세요.
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

☐ 갭 분석 3개 이상 완료
☐ 차별화 포인트 1개 이상 도출
☐ 우선순위 TOP 1 갭 명시 완료

---

## 이후 액션

| 결과 | 다음 스킬 |
|---|---|
| 갭이 크고 따라가야 할 방향 확정 | `$omc-plan` 으로 구현 설계 |
> ⛔ **AI는 위 결과를 출력한 뒤 반드시 멈춘다. 사용자가 다음 스킬을 명시적으로 호출하기 전까지 자동으로 진입하지 않는다.**

| 차별화 방향이 유망하지만 검증 필요 | `$omc-office-hours` 로 제품 관점 확인 |
| AI 확신도 낮음 항목이 다수 | 실제 제품 직접 사용 후 재실행 |

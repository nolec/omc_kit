# 외부 산출물은 성공 계약 검증 후 적용한다
날짜: 2026-08-24
태그: scheduler,provider,token-budget,atomicity

## 증상
토큰 한도 위반 child가 실패 판정됐지만 원본 patch가 남았다

## 원인
scope patch 적용이 token receipt 검증보다 먼저 실행됐다

## 적용된 규칙
return code, output 타입, token receipt 완전성 및 cap을 모두 검증한 뒤에만 원본 patch를 적용한다

## 검증 커맨드
pytest -q scripts/test_omc_n_child_scheduler.py -k 'provider_token_cap_violation or missing_token_usage'

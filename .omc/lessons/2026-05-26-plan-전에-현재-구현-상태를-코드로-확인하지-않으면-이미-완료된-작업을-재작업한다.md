# plan 전에 현재 구현 상태를 코드로 확인하지 않으면 이미 완료된 작업을 재작업한다
날짜: 2026-05-26
태그: plan,scope,duplicate-work

## 증상
T2(BLOCK → task_retry 경로)가 plan에 포함됐으나 이미 구현·테스트 완료 상태였음

## 원인
plan 수립 전 grep/git log로 현재 코드 상태를 확인하는 단계가 없었음

## 적용된 규칙
plan Phase 0에서 목표 기능 관련 코드·테스트를 grep으로 확인 후 이미 구현됐으면 범위에서 제거

## 검증 커맨드
python3 -m pytest scripts/test_omc_critique_recovery.py -q

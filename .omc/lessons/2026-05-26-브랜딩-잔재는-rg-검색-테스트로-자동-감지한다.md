# 브랜딩 잔재는 rg 검색 테스트로 자동 감지한다
날짜: 2026-05-26
태그: branding,naming,tdd,docs

## 증상
multi-assistant kit 구명칭이 7개 파일 13곳에 잔존, setup 후에도 발견 불가

## 원인
명칭 교체 시 자동 검증 수단이 없었고, untracked 파일(docs/)도 포함돼 git diff로 추적 안 됨

## 적용된 규칙
브랜딩 교체 시 test_omc_branding.py 같은 rg 검색 테스트를 먼저 작성(RED). 구명칭 패턴을 assert로 차단

## 검증 커맨드
python3 -m pytest scripts/test_omc_branding.py — 1 passed
